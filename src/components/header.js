import React, { Fragment } from 'react';

export const Header = props => {
    return (
        <Fragment>
        <nav className="nav-extended">
            <div className="nav-wrapper">
            <a href="#" className="brand-logo">Record Keeper</a>
            </div>
        </nav>
      </Fragment>
    );
}